#include <bits/stdc++.h>
using namespace std;
int main(){
    int x; // 1 iterations
    int sum = 0; // 1 iterations
    sum = x+x; //1 iterations
    // number of iterations = 3 
    int n; // 1
    cin>>n; //  1
    // O(5)
    for (int i = 0; i <n; i++)
    {
        int y;
        y = 5;
        y++;

    }
    // size of n < 100000
    // 0(5) + O(3*n)
    // total O(n)


    
    return 0;
}